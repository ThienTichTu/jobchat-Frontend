export const ChangeBbtn = (detail) => {
    return {
        type: "BTN_ACTIVE",
        payload: detail
    }
}

export const ChangePageName = (detail) => {
    return {
        type: "PAGE_NAME",
        payload: detail
    }
}